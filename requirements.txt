# Core
pandas>=2.0
numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
statsmodels>=0.14
matplotlib>=3.7            # reliability / risk-coverage figures
pydantic>=2.5
pyyaml>=6.0
tenacity>=8.2
python-dotenv>=1.0

# Data acquisition / parsing
requests>=2.31
pdfplumber>=0.11
pypdf>=4.0                 # REQUIRED: OL 316 narratives live in AcroForm fields

# Extraction backends
anthropic>=0.39            # closed-model backend
openai>=1.40               # OpenAI-compatible (vLLM) backend for open-weight models

# Optional: sentence-transformer embeddings for the T representation
# (needed only for `lift_test.py --embed sbert`, the representation-robustness
# check; pulls in torch, so left commented out of the default install)
# sentence-transformers>=2.7
